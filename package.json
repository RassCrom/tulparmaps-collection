{
  "name": "map-collection",
  "version": "1.0.0",
  "description": "High-resolution map portfolio & zoom showcase",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "node generate-maps.js && node generate-projects.js && cleancss -o style.min.css style.css && terser app.js -o app.min.js -c -m && vite build",
    "preview": "vite preview",
    "build-maps": "node generate-maps.js",
    "build:assets": "cleancss -o style.min.css style.css && terser app.js -o app.min.js -c -m"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "terser": "^5.48.0",
    "vite": "^5.2.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
